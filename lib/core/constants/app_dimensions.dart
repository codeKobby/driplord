class AppDimensions {
  // Border Radius Values
  // Use these consistently across all components for visual harmony
  
  // Large radius for main cards and containers
  static const double radiusXl = 48.0; // Floating nav bar, large containers
  static const double radiusLg = 32.0; // Main cards, large buttons
  static const double radiusMd = 16.0; // Standard cards, inputs, medium buttons
  static const double radiusSm = 8.0;  // Small cards, small buttons, chips
  static const double radiusXs = 4.0;  // Very small elements, subtle rounding
  
  // Special radius values for specific components
  static const double radiusPill = 100.0; // Full pill shape for buttons
  static const double radiusChip = 20.0;   // Filter chips and choice chips
  static const double radiusInput = 12.0;  // Input fields and text fields
  static const double radiusCard = 16.0;   // Standard card components
  static const double radiusButton = 12.0; // Standard button radius
  
  // Padding
  static const double paddingXl = 32.0;
  static const double paddingLg = 24.0;
  static const double paddingMd = 16.0;
  static const double paddingSm = 8.0;
  static const double paddingXs = 4.0;

  // Icon Sizes
  static const double iconSm = 16.0;
  static const double iconMd = 24.0;
  static const double iconLg = 32.0;
}
